import React from "react";
import {Form, Input} from "semantic-ui-react";

export function Inputs(): React.ReactElement {
	return <Form>
		<Form.Input label='Guess the eval (centipawns)' />
	</Form>
}
